namespace DemoFYP.Enums
{
    public enum OrderStatus
    {
        Pending,
        Completed,
        Cancelled
    }

    public enum PaymentStatus
    {
        Pending,
        Paid,
        Cancelled
    }
}
