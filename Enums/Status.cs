namespace DemoFYP.Enums
{
    public enum OrderStatus
    {
        Pending,
        Processing,
        Completed,
        RequestCancel,
        Cancelled
    }

    public enum PaymentStatus
    {
        Pending,
        Paid,
        Cancelled
    }
}
